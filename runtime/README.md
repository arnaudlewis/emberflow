# EmberFlow Runtime

This directory owns EmberFlow's local runtime schema and runtime-specific
documentation.

Current structure:

- `sqlite/` — canonical SQLite schema for tracks, tasks, events, and
  projections

The executable runtime logic lives in `src/` at the EmberFlow crate root.
